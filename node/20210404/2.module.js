// 全局安装只能在命令行里用：自己实现全局包
// 1、需要配置bin命令
// 2、添加执行方式#！ /usr/bin/env node
// 3、将此包安装到npm下（可以全局安装）临时做一个npm link（为了调试方便）
// => 工具类的使用全局包

// 本地安装，在代码中使用
// 依赖关系（开发环境 --webpack gulp 生产依赖 vue）同等依赖 打包依赖 可选依赖
// npm install 模版 --save --save-dev（-D）

// npm
